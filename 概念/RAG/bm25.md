---
title: "BM25"
category: -concepts
tags: ["rag", "information-retrieval", "keyword-search", "sparse-retrieval"]
summary: "BM25 是一种经典的关键词检索排序函数，基于词频和逆文档频率，广泛用于搜索引擎和 RAG 的关键词召回。"
created: 2026-06-26
updated: 2026-07-21
tier: supporting
aliases:
  - "Best Match 25"
relationships:
  - target: "概念/RAG/hybrid-search"
    type: used_by
  - target: "概念/RAG/vector-database"
    type: complements
sources:
  - "https://en.wikipedia.org/wiki/Okapi_BM25"
name_zh: "经典关键词排序算法"
---

# BM25

> 中文简称：经典关键词排序算法

> **一句话理解**: BM25 是按「关键词在文档里出现多少次、在多少文档里出现过」来给文档打分的经典检索算法。

## 核心原理

### 基于 TF-IDF 的改进

BM25 是 TF-IDF 的改进版，增加了两个关键改进：
- **词频饱和**：词频增加时分数增长递减（而非线性增长）
- **文档长度归一化**：长文档不会因词多而天然占优

### 公式

```text
score(D, Q) = Σ IDF(q_i) × (f(q_i,D) × (k1+1)) / (f(q_i,D) + k1×(1-b+b×|D|/avgDL))

其中：
  f(q_i, D) = 词 q_i 在文档 D 中的词频
  |D|       = 文档长度
  avgDL     = 平均文档长度
  k1        = 词频饱和参数 (1.2-2.0)
  b         = 长度归一化参数 (0-1, 通常 0.75)
  IDF(q_i)  = log((N - n(q_i) + 0.5) / (n(q_i) + 0.5))
```

### 参数说明

| 参数 | 典型值 | 作用 |
|------|--------|------|
| **k1** | 1.2-2.0 | 控制词频饱和度，越大词频影响越大 |
| **b** | 0.75 | 控制文档长度影响，0=不考虑长度，1=完全归一化 |

## BM25 vs 向量检索

| 维度 | BM25 | 向量检索 |
|------|------|----------|
| 匹配方式 | 精确关键词匹配 | 语义相似度 |
| 优势 | 专有名词、精确查询 | 同义词、语义理解 |
| 劣势 | 不理解语义 | 可能丢失精确匹配 |
| 速度 | 极快（倒排索引） | 较快（ANN 索引） |
| 可解释性 | 强（知道哪个词匹配） | 弱（向量距离） |
| 典型引擎 | Elasticsearch, OpenSearch | Milvus, Qdrant, Pinecone |

## 在 RAG 中的角色

```
用户查询
    │
    ├──→ BM25 关键词召回 (Top-K₁)
    │         │
    │         └──→ 融合排序 (RRF) ──→ Top-K 结果 ──→ LLM 生成
    │         │
    └──→ 向量语义召回 (Top-K₂)
```

BM25 在混合检索中补充向量检索的不足：
- 精确产品名/型号查询（如 "RTX 4090"）
- 错误代码/异常名称（如 "NullPointerException"）
- 人名、地名等专有名词

## 实战示例

### Python (rank_bm25)

```python
from rank_bm25 import BM25Okapi
import jieba

# 中文分词
corpus = ["机器学习是人工智能的分支", "深度学习使用神经网络", "BM25是经典检索算法"]
tokenized_corpus = [list(jieba.cut(doc)) for doc in corpus]

bm25 = BM25Okapi(tokenized_corpus)

# 查询
query = "检索算法"
tokenized_query = list(jieba.cut(query))
scores = bm25.get_scores(tokenized_query)
# 返回: [0.0, 0.0, 0.69]  → 第三篇文档最相关
```

### Elasticsearch

```json
{
  "query": {
    "match": {
      "content": {
        "query": "RAG 检索增强生成",
        "operator": "or"
      }
    }
  }
}
```

## 最佳实践

1. **与向量检索组合**：单独 BM25 不够，混合检索效果最佳
2. **中文分词**：使用 jieba/IK 分词器，而非按字切分
3. **参数调优**：k1=1.2, b=0.75 是通用起点
4. **停用词过滤**：移除"的"、"是"等无信息量词
5. **RRF 融合**：用 Reciprocal Rank Fusion 合并 BM25 和向量结果

## Related

- [[概念/RAG/hybrid-search|Hybrid Search]] — BM25 + 向量的混合检索
- [[概念/RAG/vector-database|Vector Database]] — 语义检索的存储层
- [[概念/RAG/hnsw|HNSW]] — 向量索引算法
- [[14_RAG系统/01_RAG基础/06_RAG基础|RAG 基础]] — 检索增强生成
- [[14_RAG系统/04_高级RAG/14_RAG_检索_延迟_优化|检索延迟优化]]

## 2026 BM25 生态现状

| 实现 | 语言 | 特点 | 状态 |
|------|------|------|------|
| Elasticsearch | Java | 分布式、成熟 | ✅ 主流 |
| OpenSearch | Java | 云原生 | ✅ 成熟 |
| rank_bm25 | Python | 轻量、原型 | ✅ 成熟 |
| Tantivy | Rust | 高性能 | ✅ 成熟 |
| Meilisearch | Rust | 即时搜索 | ✅ 成熟 |
| Typesense | C++ | 低延迟 | ✅ 成熟 |

## BM25 vs 向量检索对比

| 维度 | BM25 | 向量检索 | 混合 |
|------|------|------|------|
| 精确匹配 | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐⭐ |
| 语义理解 | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 速度 | 极快 | 快 | 中 |
| 可解释性 | 高 | 低 | 中 |
| 多语言 | 需分词 | 原生支持 | 原生支持 |

## 检查清单

- [ ] 分词器已配置（中文用 jieba/IK）
- [ ] 停用词表已配置
- [ ] BM25 参数已调优（k1/b）
- [ ] 与向量检索已融合
- [ ] 性能已测试
- [ ] 索引已优化

## 常见问题

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 中文检索差 | 未分词 | 配置 jieba/IK 分词器 |
| 召回率低 | 参数不当 | 调整 k1=1.2, b=0.75 |
| 与向量结果冲突 | 权重不当 | RRF 融合 + 权重调优 |
| 索引慢 | 数据量大 | 批量索引 + 异步 |

## 延伸阅读

- [[概念/RAG/hybrid-search|Hybrid Search]] — 混合检索
- [[概念/RAG/vector-database|Vector Database]] — 向量数据库
- [[概念/RAG/reranker|Reranker]] — 重排序
- [[概念/RAG/retrieval-latency|Retrieval Latency]] — 检索延迟
- [[14_RAG系统/01_RAG基础/06_RAG基础|RAG 基础]]

> ℹ️ BM25 是关键词检索的经典算法，2026年仍是混合检索的必备组件，与向量检索互补，在精确匹配场景不可替代。

## BM25 参数调优参考

| 参数 | 默认值 | 说明 | 调优建议 |
|------|------|------|------|
| k1 | 1.2 | 词频饱和度 | 1.0-2.0 |
| b | 0.75 | 文档长度归一化 | 0.5-0.9 |
| delta | 0 | 额外奖励 | 0-1 |

## 中文分词器对比

| 分词器 | 速度 | 精度 | 适用场景 |
|------|------|------|------|
| jieba | 快 | 中 | 通用 |
| IK | 中 | 高 | ES 集成 |
| pkuseg | 慢 | 高 | 领域适配 |
| HanLP | 中 | 高 | 多语言 |

> ℹ️ BM25 在 2026 年仍是稀疏检索的基石，与向量检索组合的 Hybrid Search 已成为生产 RAG 的标配方案。

## 延伸阅读

- [[概念/RAG/hybrid-search|Hybrid Search]] — 混合检索
- [[概念/RAG/hnsw|HNSW]] — 向量索引
- [[概念/RAG/reranker|Reranker]] — 重排序
