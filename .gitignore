# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files
.env
.env.local
Web/.env
Web/.eval-history

# STAROps private config (contains credentials)
.starops/config.json
.starops/*.json
!.starops/*.template

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# 同步工具重复文件防护（iCloud/编辑器产生的副本）
# 覆盖 "* 2.md" / "file 2" / "pre-commit 3" / "*conflicted copy*" / "*.copy" 等
* 2.md
* 2.json
* 2.txt
* 2.pdf
* 2.svg
* 2.html
* 2.xml
* [0-9]
* [0-9].*
*.copy
*.copy.md
*conflicted copy*
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python
.venv
__pycache__
*.pyc
.pytest_cache/

# AI 工具配置目录（不需要入库）
.comate/
.crush/
.qoder/
.qwen/
.mimocode/
.mimosa/

# 备份文件
*.bak

# macOS 系统文件
.DS_Store


# ============================================================
# _raw/ — GitHub source mirrors (staging area for wiki distillation)
# Strategy: ALLOWLIST. Only .md / .ipynb / .py knowledge files pass.
# Original 13581 files / 1.35 GB  →  target ~700 files / <15 MB.
# ============================================================

# Step 1 — Default deny: ignore everything under _raw/...
_raw/**

# ...but keep directory structure traversable (so git descends into dirs)
!_raw/**/

# Step 2 — Allowlist the three knowledge extensions
!_raw/**/*.md
!_raw/**/*.ipynb
!_raw/**/*.py

# Step 3 — Defense-in-depth: kill nested .git even if rules above change
_raw/**/.git/
_raw/**/.git
_raw/**/.gitmodules
_raw/**/.gitattributes

# Step 4 — Translations: keep English originals only.
# (translations/*/ re-ignores all language subdirs that Step 2 may have allowed;
#  then we re-allow en/ — last matching pattern wins.)
_raw/github-sources/ai-agents-for-beginners/translations/*/
!_raw/github-sources/ai-agents-for-beginners/translations/en/

# ============================================================
# 通用开发与运行时产物
# ============================================================

# Node modules and build artifacts
前端应用/node_modules/
前端应用/dist/
前端应用/.vite/

# Python cache
__pycache__/
*.pyc
*.pyo
.venv/

# OS files
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Obsidian
.obsidian/workspace
.obsidian/workspace-mobile.json

# Large archives
*.tar.gz
*.zip

# Empty release staging
release_new/
code/
.code-up/rag-index/
